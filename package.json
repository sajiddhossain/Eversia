{
  "name": "eversia",
  "scripts": {
    "deploy": "cd client && npm run build && cd .. && npx firebase-tools deploy --non-interactive",
    "test": "npx firebase-tools emulators:exec --only firestore \"npm run test:run\"",
    "test:run": "jest --runInBand --detectOpenHandles --forceExit"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.11.0",
    "firebase": "^12.10.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
